version: '3.8'

services:
  notification-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: notification-service
    ports:
      - "50051:50051"
    environment:
      - NODE_ENV=production
      - GRPC_HOST=0.0.0.0
      - GRPC_PORT=50051
      - GRPC_USE_TLS=false
      - SERVICE_NAME=notification-service
      - SERVICE_VERSION=1.0.0
      - DEFAULT_RETRY_COUNT=3
      - DEFAULT_TIMEOUT_MS=5000
    restart: unless-stopped
    networks:
      - microservices

  # Пример API Gateway (для демонстрации)
  # api-gateway:
  #   build:
  #     context: ../api-gateway
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NOTIFICATION_SERVICE_URL=notification-service:50051
  #   depends_on:
  #     - notification-service
  #   networks:
  #     - microservices

networks:
  microservices:
    driver: bridge

